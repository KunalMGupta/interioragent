# <Scene type> — example

Copy this when starting a new scene type. Fill it as you build; it is the
distilled, reusable recipe for this kind of room.

## Prompt(s) this covers
- "<example prompt>"

## Plan summary
What the planner produced (look, materials, mood). Key anchors / secondary /
wall-decor items. Link the plan image if kept.

## Skeleton program
The working structure (groups + placement), phase by phase. Keep it minimal and
correct — the canonical starting point, not a full dump.

```python
# Phase 1 — floor anchors
...
# Phase 2 — surface & floor details
...
# Phase 3 — walls, ceiling & decor
...
```

## What worked / gotchas
- Layout choices that read well.
- Asset retrieval queries that returned good meshes (and bad ones to avoid).
- Placement verbs that did what we wanted.

## VLM feedback we hit and how we resolved it
- feedback → action → result. (Mirror the notable ones into
  ../workflow/vlm_feedback.md too.)

## Manual constraints used
- e.g. Clearance/Access/Visibility and why.
